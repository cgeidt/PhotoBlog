<h2><%= @album.title %></h2>
<p>
  <%= @album.description %>
</p>
<p>
  <br/>
  <%= link_to t('btn_back'), albums_path, class: 'btn btn-small btn-info' %>
  <%= link_to t('to_home'), welcome_index_path, class: 'btn btn-small btn-info' %>
  <%= link_to t('btn_add_photo'), new_album_photo_path(@album), class: 'btn btn-small' %>
  <br/>
</p>

<hr>
<ul class="list-group">
  <%= render @album.photos %>
</ul>
<hr>
